import { makeStyles } from "@material-ui/core/styles";
export default makeStyles((theme) => ({
  postsContainer: {
    marginTop: "120px",
  },

  mainContainer: {
    display: "flex",
    marginTop: "100px",
    alignItems: "center",
  },
  smMargin: {
    margin: theme.spacing(1),
  },
  actionDiv: {
    textAlign: "center",
  },
}));
